import Book from '../Book';
import BookForm from '../BookForm';

const Home = () => (
  <div className="book-card-list">
    <Book />
    <Book />
    <Book />
    <BookForm />
  </div>
);

export default Home;
